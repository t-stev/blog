---
title: Hello, world
date: 2026-07-03
---

This is the first post. It exists mostly to prove the blog works.

Posts are Markdown files in `content/posts/`. To write a new one, add a file
like this one with a `title` and `date` in the front matter, then commit and
push — the site rebuilds and deploys automatically.

Some formatting checks:

- Lists work
- **Bold** and *italic* work
- `inline code` works

```python
def hello():
    print("and code blocks work too")
```

> Blockquotes as well.
